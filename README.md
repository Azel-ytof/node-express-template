# Node + Express

NodeJS + Express only starter/template/boilerplate/bootstrap.

Clone this repository or click "Use this template" button in the to right corner on the main page.

Remember to run `npm install` after cloning to retrieve dependencies.

/!\ Do not forget to replace `name` in [package.json](./package.json) with your project name /!\
